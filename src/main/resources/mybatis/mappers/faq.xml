<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.dao.FaqDAO">

    <select id="list" resultType="com.boot.dto.FaqDTO">
        SELECT faqNo, faqCategory, faqTitle, faqContent, faqDate
        , TO_CHAR(faqDate, 'YYYY-MM-DD HH24:MI') AS faqDate2
        FROM tbl_faq
        ORDER BY faqNo DESC
    </select>
    
    <insert id="write">
        INSERT INTO tbl_faq(faqNo, faqCategory, faqTitle, faqContent) 
        VALUES(faq_seq.NEXTVAL, #{faqCategory}, #{faqTitle}, #{faqContent})
    </insert>
    
    <select id="contentView" resultType="com.boot.dto.FaqDTO">
        SELECT faqNo, faqCategory, faqTitle, faqContent, faqDate
        , TO_CHAR(faqDate, 'YYYY-MM-DD HH24:MI') AS faqDate2
        FROM tbl_faq
        WHERE faqNo = #{faqNo}
    </select>

    <update id="modify">
        UPDATE tbl_faq 
        SET faqCategory=#{faqCategory}, faqTitle=#{faqTitle}, faqContent=#{faqContent} 
        WHERE faqNo=#{faqNo}
    </update>
    
    <delete id="delete">
        DELETE FROM tbl_faq 
        WHERE faqNo=#{faqNo}
    </delete>

</mapper>