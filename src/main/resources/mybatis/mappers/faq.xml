<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.dao.FaqDAO">

  <!-- 컬럼 ↔ DTO 필드 1:1 매핑 -->
  <resultMap id="FaqMap" type="com.boot.dto.FaqDTO">
    <id     column="FAQNO"        property="faq_id"/>
    <result column="FAQCATEGORY"  property="category"/>
    <result column="FAQTITLE"     property="question"/>
    <result column="FAQCONTENT"   property="answer"/>
    <result column="CREATED_AT"   property="created_at"/>
  </resultMap>

  <!-- 목록 -->
  <select id="listWithPaging" resultMap="FaqMap">
  SELECT faqNo,      
         faqCategory, 
         faqTitle,   
         faqContent,               
         TO_CHAR(FAQDATE, 'YYYY-MM-DD') AS CREATED_AT
  FROM (
      SELECT FAQNO, FAQCATEGORY, FAQTITLE, FAQCONTENT, FAQDATE, ROWNUM AS rnum
    FROM (
      SELECT FAQNO, FAQCATEGORY, FAQTITLE, FAQCONTENT, FAQDATE
      FROM TBL_FAQ
      ORDER BY FAQNO DESC
    )
    WHERE ROWNUM &lt;= #{pageNum} * #{amount}
  )
  WHERE rnum > (#{pageNum} - 1) * #{amount}
</select>


  <select id="getTotalCount" resultType="int">
    SELECT COUNT(*) FROM TBL_FAQ
  </select>

  <!-- 상세 (단일 파라미터는 value로 받거나 @Param 사용) -->
  <select id="getFaq" resultMap="FaqMap">
    SELECT FAQNO,
           FAQCATEGORY,
           FAQTITLE,
           FAQCONTENT,
           TO_CHAR(FAQDATE, 'YYYY-MM-DD HH24:MI') AS CREATED_AT
    FROM TBL_FAQ
    WHERE FAQNO = #{value}   <!-- getFaq(..)의 단일 인자 -->
  </select>

  <insert id="write" parameterType="com.boot.dto.FaqDTO">
    <selectKey keyProperty="faq_id" resultType="long" order="BEFORE">
      SELECT FAQ_SEQ.NEXTVAL FROM DUAL
    </selectKey>
    INSERT INTO TBL_FAQ (FAQNO, FAQCATEGORY, FAQTITLE, FAQCONTENT)
    VALUES (#{faq_id}, #{category}, #{question}, #{answer})
  </insert>

  <update id="modify" parameterType="com.boot.dto.FaqDTO">
    UPDATE TBL_FAQ
    SET FAQCATEGORY = #{category},
        FAQTITLE    = #{question},
        FAQCONTENT  = #{answer},
        FAQDATE     = SYSDATE
    WHERE FAQNO = #{faq_id}
  </update>

  <delete id="delete">
    DELETE FROM TBL_FAQ WHERE FAQNO = #{value}   <!-- 단일 인자 -->
  </delete>

</mapper>
