<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.dao.NoticeDAO">
    <select id="listWithPaging" resultType="com.boot.dto.NoticeDTO">
        SELECT notice_id, title, views, created_at, is_urgent
        FROM (
            SELECT notice_id, title, views, created_at, is_urgent, ROWNUM as rnum
            FROM (
                SELECT notice_id, title, views, TO_CHAR(created_at, 'YYYY-MM-DD') as created_at, is_urgent
                FROM RECALL_NOTICE_DB
                ORDER BY notice_id DESC
            )
            WHERE ROWNUM &lt;= #{pageNum} * #{amount}
        )
        WHERE rnum > (#{pageNum} - 1) * #{amount}
    </select>

    <select id="getTotalCount" resultType="int">
        SELECT count(*) FROM RECALL_NOTICE_DB
    </select>

    <insert id="write">
        INSERT INTO RECALL_NOTICE_DB (title, content, is_urgent) VALUES (#{title}, #{content}, NVL(#{is_urgent, jdbcType=CHAR}, 'N'))
    </insert>

    <select id="getNotice" resultType="com.boot.dto.NoticeDTO">
        SELECT notice_id, title, content, views, TO_CHAR(created_at, 'YYYY-MM-DD HH24:MI') as created_at, is_urgent
        FROM RECALL_NOTICE_DB
        WHERE notice_id = #{notice_id}
    </select>

    <update id="incrementViews">
        UPDATE RECALL_NOTICE_DB SET views = views + 1 WHERE notice_id = #{notice_id}
    </update>

    <update id="modify">
        UPDATE RECALL_NOTICE_DB SET title = #{title}, content = #{content}, is_urgent = #{is_urgent}, updated_at = SYSDATE WHERE notice_id = #{notice_id}
    </update>

    <delete id="delete">
        DELETE FROM RECALL_NOTICE_DB WHERE notice_id = #{notice_id}
    </delete>
</mapper>