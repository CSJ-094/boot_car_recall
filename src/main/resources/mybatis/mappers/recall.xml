<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.dao.RecallDAO">

    <insert id="insertRecallList">
        INSERT INTO RECALL (
        ID, MAKER, MODEL_NAME, RECALL_START, RECALL_END,
        RECALL_DATE, RECALL_REASON
        )
        VALUES (
        RECALL_SEQ.NEXTVAL,
        #{maker}, #{modelName}, #{recallStart}, #{recallEnd},
        #{recallDate}, #{recallReason}
        )
    </insert>

    <select id="selectAll" resultType="com.boot.dto.RecallDTO">
        SELECT * FROM RECALL
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM RECALL
    </select>

</mapper>