<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.dao.RecallDAO">

    <insert id="insertRecall" parameterType="com.boot.dto.RecallDTO">
        INSERT INTO RECALL (
        ID, MAKER, MODEL_NAME, MAKE_START, MAKE_END,
        RECALL_DATE, RECALL_REASON
        ) VALUES (
        RECALL_SEQ.NEXTVAL,
        #{maker}, #{modelName}, #{makeStart}, #{makeEnd},
        #{recallDate}, #{recallReason}
        )
    </insert>

    <insert id="insertRecallList" parameterType="java.util.List">
        INSERT INTO RECALL (
        ID, MAKER, MODEL_NAME, MAKE_START, MAKE_END,
        RECALL_DATE, RECALL_REASON
        )
        <foreach collection="list" item="recall" separator="UNION ALL">
            SELECT
            RECALL_SEQ.NEXTVAL,
            #{recall.maker}, #{recall.modelName}, #{recall.makeStart}, #{recall.makeEnd},
            #{recall.recallDate}, #{recall.recallReason}
            FROM DUAL
        </foreach>
    </insert>


    <select id="selectAll" resultType="com.boot.dto.RecallDTO">
        SELECT
        ID, MAKER, MODEL_NAME, MAKE_START, MAKE_END, RECALL_DATE, RECALL_REASON
        FROM RECALL
        <where>
            <if test="keyword != null and keyword != ''">
                UPPER(MAKER) LIKE '%' || UPPER(#{keyword}) || '%'
                OR UPPER(MODEL_NAME) LIKE '%' || UPPER(#{keyword}) || '%'
            </if>
        </where>
        ORDER BY ID DESC
        OFFSET #{offset} ROWS FETCH NEXT #{amount} ROWS ONLY
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM RECALL
        <where>
            <if test="keyword != null and keyword != ''">
                UPPER(MAKER) LIKE '%' || UPPER(#{keyword}) || '%'
                OR UPPER(MODEL_NAME) LIKE '%' || UPPER(#{keyword}) || '%'
            </if>
        </where>
    </select>

    <select id="searchByModelName" parameterType="string" resultType="com.boot.dto.RecallDTO">
        SELECT
        ID, MAKER, MODEL_NAME, MAKE_START, MAKE_END, RECALL_DATE, RECALL_REASON
        FROM RECALL
        WHERE UPPER(MODEL_NAME) LIKE '%' || UPPER(#{modelName}) || '%'
        ORDER BY RECALL_DATE DESC
    </select>

</mapper>